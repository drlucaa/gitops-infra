apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: secret-test
spec:
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  target:
    name: secret-test
  data:
    - secretKey: username
      remoteRef:
        key: "test-secret"
        property: "username"
    - secretKey: password
      remoteRef:
        key: "test-secret"
        property: "password"
